type Result_1 = 
 variant {
   err: text;
   ok: text;
 };
type Result = 
 variant {
   err: text;
   ok: nat;
 };
service : {
  getMarkdown: (nat) -> (Result_1) query;
  saveMarkdown: (text) -> (Result);
}
